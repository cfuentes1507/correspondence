<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="correspondence_document_view_kanban" model="ir.ui.view">
            <field name="name">correspondence.document.kanban</field>
            <field name="model">correspondence_document</field>
            <field name="arch" type="xml">
                <kanban default_group_by="state" class="o_kanban_mobile">
                    <field name="correlative"/>
                    <field name="name"/>
                    <field name="date"/>
                    <field name="send_department_id"/>
                    <field name="recipient_department_ids"/>
                    <field name="state"/>
                    <field name="user_facing_state"/>
                    <field name="correspondence_type"/>
                    <templates>
                        <t t-name="kanban-box">
                            <div t-attf-class="oe_kanban_global_click">
                                <div class="o_kanban_record_top mb-2">
                                    <div class="o_kanban_record_headings">
                                        <strong class="o_kanban_record_title">
                                            <span><t t-esc="record.name.value"/></span>
                                        </strong>
                                        <small class="o_kanban_record_subtitle text-muted">
                                            <t t-esc="record.correlative.value"/>
                                        </small>
                                    </div>
                                </div>
                                <div class="o_kanban_record_body">
                                    <div class="mb-2">
                                        <i class="fa fa-calendar-o" aria-label="Fecha" title="Fecha"/> <t t-esc="record.date.value"/>
                                    </div>
                                    <div class="mb-2">
                                        <i class="fa fa-paper-plane-o" aria-label="Remitente" title="Remitente"/> <t t-esc="record.send_department_id.value"/>
                                    </div>
                                    <div class="mb-2">
                                        <i class="fa fa-users" aria-label="Destinatarios" title="Destinatarios"/> <t t-esc="record.recipient_department_ids.value"/>
                                    </div>
                                </div>
                                <div class="o_kanban_record_bottom">
                                    <div class="oe_kanban_bottom_left">
                                        <span class="oe_kanban_list_many2many"><t t-esc="record.correspondence_type.value"/></span>
                                    </div>
                                    <div class="oe_kanban_bottom_right"><span t-attf-class="badge #{['draft', 'signed'].indexOf(record.state.raw_value) > -1 ? 'badge-secondary' : 'badge-success' }"><t t-esc="record.user_facing_state.value"/></span></div>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>
    </data>
</odoo>